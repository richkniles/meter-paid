<h1><%= "Update your profile"%></h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for(@user) do |f| %>

			<%= render 'user_fields', f: f %>

			<%= f.submit "Save changes" %>
	
		<% end %>
	</div>
</div>
